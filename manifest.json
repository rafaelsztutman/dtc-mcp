{
  "manifest_version": "0.3",
  "name": "dtc-mcp",
  "display_name": "DTC E-Commerce Analytics",
  "version": "1.0.3",
  "description": "Code-execution MCP for Klaviyo + Shopify analytics. Three tools (execute_code, search_docs, read_doc) backed by typed SDKs in a stateful V8 sandbox.",
  "author": {
    "name": "Rafael Sztutman"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/rafaelsztutman/dtc-mcp"
  },
  "homepage": "https://github.com/rafaelsztutman/dtc-mcp#readme",
  "keywords": ["klaviyo", "shopify", "dtc", "ecommerce", "code-execution", "sandbox"],
  "privacy_policies": [
    "https://github.com/rafaelsztutman/dtc-mcp/blob/main/PRIVACY.md"
  ],
  "icon": "icon.png",
  "server": {
    "type": "node",
    "entry_point": "server/bootstrap.cjs",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/bootstrap.cjs"],
      "env": {
        "KLAVIYO_API_KEY": "${user_config.klaviyo_api_key}",
        "SHOPIFY_STORE": "${user_config.shopify_store}",
        "SHOPIFY_CLIENT_ID": "${user_config.shopify_client_id}",
        "SHOPIFY_CLIENT_SECRET": "${user_config.shopify_client_secret}",
        "SHOPIFY_ACCESS_TOKEN": "${user_config.shopify_access_token}"
      }
    }
  },
  "user_config": {
    "klaviyo_api_key": {
      "type": "string",
      "title": "Klaviyo API Key",
      "description": "Your Klaviyo private API key (starts with pk_). Find it at Settings > Account > API Keys.",
      "sensitive": true,
      "required": true
    },
    "shopify_store": {
      "type": "string",
      "title": "Shopify Store Domain",
      "description": "Your Shopify store domain (e.g., your-store.myshopify.com). Leave empty for Klaviyo-only mode.",
      "required": false
    },
    "shopify_client_id": {
      "type": "string",
      "title": "Shopify Client ID",
      "description": "App Client ID from the Shopify Partners Dashboard (for Dev Dashboard apps).",
      "required": false
    },
    "shopify_client_secret": {
      "type": "string",
      "title": "Shopify Client Secret",
      "description": "App Client Secret from the Shopify Partners Dashboard (starts with shpss_).",
      "sensitive": true,
      "required": false
    },
    "shopify_access_token": {
      "type": "string",
      "title": "Shopify Access Token (Legacy)",
      "description": "Admin API access token for legacy custom apps (starts with shpat_). Use this OR Client ID/Secret, not both.",
      "sensitive": true,
      "required": false
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": ["darwin", "win32"],
    "runtimes": {
      "node": ">=20.0.0"
    }
  },
  "tools": [
    {
      "name": "execute_code",
      "description": "Run TypeScript-like JavaScript against typed Klaviyo + Shopify SDKs in a stateful V8 sandbox. Variables on globalThis persist across calls within the same MCP connection."
    },
    {
      "name": "search_docs",
      "description": "Search SDK reference docs for Klaviyo and Shopify methods. Use when discovering methods by intent."
    },
    {
      "name": "read_doc",
      "description": "Fetch one docs chunk by path, or list all available paths. Use when you already know the chunk ID — faster than search_docs."
    }
  ]
}
