{
  "id": "01-list-recent-campaigns",
  "category": "single-fact",
  "prompt": "List the 5 most recently sent email campaigns from this Klaviyo account. Put the answer inside `final_answer` as a JSON array of exactly 5 objects, each with fields: id, name, send_time. Do not include any other prose in final_answer.",
  "claims": [
    { "type": "list-size", "expected": 5, "description": "exactly 5 campaigns returned" },
    { "type": "fields-present", "expected": ["id", "name", "send_time"], "description": "each item has id, name, send_time" },
    { "type": "ordering", "expected": "descending", "description": "ordered by send_time descending" }
  ],
  "notes": "Single tool call should suffice (get_campaigns with filter + sort)."
}
