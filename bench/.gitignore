node_modules/
*.tsbuildinfo
notes/probe-results/

# All per-run artifacts. The bench runs against a real Klaviyo account, so
# state.json / raw.jsonl contain real campaign IDs, flow names, etc. — not
# secrets, but not the kind of thing you'd voluntarily publish either.
# Only the final aggregate report (committed manually as report.md after
# review + any needed redaction) should ever land in git.
results/

# Local MCP tool-list dump (optional, may contain identifiers).
tools-list-dump.json
